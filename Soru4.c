//#include <stdio.h>

//int main(){
   // int a = 5;
  //  int b = 5 , c = 7;

  //  printf("%d", a << 2);
    //Burada ikili tabanda sayı gösterme ekana yazdırılır.
  //  printf("\nİkili Taban sistemi^^^");

    //printf("\n%d",a+b * 2);
  //  printf("\nİşlem önceliği sistemi^^^");
    //Burada işlem önceliği vardır yani ilk olarak b ile 2 çarpılacak sonra a ile toplanacak.

   // printf("\n%d", a%b);
  //  printf("\nKalan bulma sistemi^^^");
    //Burada a değişkeni ile b değişkeninin bölümünden kalan kaçtır deniliyor.

    //printf("\n%d",a == b);
    //printf("\nEşitse sistemi True^^^");
   // printf("\n%d", a == c);
  //  printf("\nEşitse sistemi False^^^");
    //Burada a değişkeni ile b değişkeninin eşitliğini sorguluyor eğer a ile b değişkenleri
    // aynı ise True(1,'0' dışındakı her şey True'dur.) farklı ise False(0) verecektir.

    //printf("\n%d", a > c);
   // printf("\nKüçükse False^^^");
    //Burada False verecektir.
   // printf("\n%d",c > a);
  //  printf("\nBüyüktür True^^^");
    //Burada True verecektir.
//    return 0;

//}